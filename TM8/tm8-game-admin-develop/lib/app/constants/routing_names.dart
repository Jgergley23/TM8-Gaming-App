const login = '/';

const home = '/users';

const forgotPasswordEmail = '/forgot-password/email';

const forgotPasswordVerificationCode = '/forgot-password/verification-code';

const forgotPasswordReset = '/forgot-password/reset';

const forgotPasswordConformation = '/forgot-password/conformation';

const notifications = '/notifications';

const manageAdmins = '/manage-admins';

const usersDetails = '/users/:userId';

const notificationAdd = '/notifications/add';

const notificationDetails = '/notifications/:notificationId';
